{
  "name": "qagarden",
  "version": "3.6.0",
  "private": true,
  "description": "QAGarden role-based QA checklist with a bold editorial gallery interface; authentication and storage logic preserved",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
