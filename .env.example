# Required on Vercel. These are normally injected automatically after
# connecting an Upstash Redis integration to the Vercel project.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: use a separate Redis namespace for preview/staging deployments.
QAGARDEN_NAMESPACE=qagarden
